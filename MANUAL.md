# MAD 语言操作手册

MAD（**MAD Ain't Disciplined**）是一门刻意为小、以后缀记法（逆波兰）+ 数据栈为核心的
原型语言。本手册是语言的完整参考：词法、类型、内建字（底层函数）、控制流、函数、
指针与内存模型、实现原理以及推荐代码风格。

> 配套资料：快速入门见根目录 `README.md`；可运行的完整程序见 `examples/`。

---

## 目录

1. [心智模型](#1-心智模型)
2. [词法结构](#2-词法结构)
3. [类型系统](#3-类型系统)
4. [变量与作用域](#4-变量与作用域)
5. [内建字参考](#5-内建字参考)
6. [控制流与标签](#6-控制流与标签)
7. [函数](#7-函数)
8. [指针](#8-指针)
9. [内存模型](#9-内存模型)
10. [类型转换](#10-类型转换)
11. [输入输出细节](#11-输入输出细节)
12. [错误处理](#12-错误处理)
13. [执行模型与实现原理](#13-执行模型与实现原理)
14. [代码风格指南](#14-代码风格指南)
15. [完整示例](#15-完整示例)

---

## 1. 心智模型

MAD 没有"语句"和"表达式"之分：**一切都是数据栈上的操作**。

- 每个**字面量**（`3`、`"hi"`、`u:7`……）把一个值压栈；
- 每个**字**（word，如 `dup`、`+`、`print`、变量名、函数名）执行一次操作，
  通常是从栈顶弹出若干操作数、压回结果；
- 执行就是从头到尾线性消费 token 流，遇到跳转才改变位置。

因此 `a b +` 的意思是：压入 `a` 的值、压入 `b` 的值、执行 `+`
（弹出两个数相加，压回和）。书写顺序即执行顺序，没有优先级。

本手册用栈效应记号描述内建字：

```text
名字        ( 弹出顺序 -- 压入顺序 )
+           ( a b -- sum )        // 注意：b 先弹
```

---

## 2. 词法结构

### 2.1 注释与空白

```text
// 行注释，到行尾为止
```

空白和换行只是 token 分隔符。源文件按行号记录，用于错误诊断。

### 2.2 圆括号 `( )`：纯视觉分组

`(` 和 `)` 被**词法器直接丢弃**，不产生任何语义。它们纯粹是给人看的分组符号：

```text
((a b +) c *)
(a b +) c *
a b + c *
```

三者完全等价。风格约定见第 14 节。

### 2.3 方括号 `[ ]`：参数组

`[` `]` 有语义：收集其中的 token 并**整体反转**后放回 token 流，末尾附加一个组结束标记。
它只用于函数参数表（见 7.2），**不可嵌套**。反转是为了让

```text
:add [a b]
```

配合调用 `x y add` 时 `a` 绑定 `x`、`b` 绑定 `y`——即书写顺序与绑定顺序一致。

### 2.4 标识符

合法变量名以字母或 `_` 开头，后续字符可以是字母、数字、`_`、`@`。
`@` 允许出现在名字里是为了支持 `x@i64` 这种带类型注解的名字（见 2.7）。

以下前缀记号由词法器作为**独立 token**识别：

| 形式 | 含义 |
|------|------|
| `&name` | 引用：变量→指针，标签→label 值，函数→func 值 |
| `*name` | 解引用：加载指针变量所指的值（单独的 `*` 是乘法） |
| `!@T` | 类型转换 cast |
| `name:` | 标签定义（`:` 结尾的普通词） |
| `u:N` | 无符号字面量 |

### 2.5 字面量

| 形式 | 类型 | 说明 |
|------|------|------|
| `123`、`-3`、`+5` | i64 | 可选正负号的十进制整数 |
| `3.14`、`-0.5` | f64 | 含一个小数点的数 |
| `u:42` | u64 | `u:` 前缀的无符号整数 |
| `"text"` | memptr | 字符串，见 9.4 |

注意：因为 `-3` 整体被识别为一个整数字面量，`x 1 -`（减法）没问题，
但 `x -3 +` 里的 `-3` 是负三而不是"负号作用于 3"。对变量取负请写 `(0 x -)`。

字符串支持转义 `\n` `\r` `\t` `\0` `\\` `\"`；其他转义保留转义符后的字符本身。
单个 token（含字符串原始内容）最长 255 字节。

### 2.6 冒号、分号与全局捕获

```text
:name ... ;            // 函数定义，见第 7 节
:{a b}name ... ;       // 带全局捕获的函数定义
;                      // 顶层的分号：到此停止执行（其后内容不运行）
```

`:{...}` 的 `{}` 是**编译期元数据**，不是运行期操作；`:`、`{`、名字表之间不能有空格。

### 2.7 类型注解 `name@type`

任何变量名都可以携带 `@type` 后缀固定其类型：

```text
3 x@i64        // 声明 x 为 i64
```

类型拼写见第 3 节；`f64` 的别名 `double` 同样有效。注解一旦生效便永久固定，
之后带注解的使用必须与之一致（不带注解的使用不受限）。

---

## 3. 类型系统

### 3.1 运行时类型

| 类型 | 含义 | 字面量/产生方式 |
|------|------|----------------|
| `i64` | 64 位有符号整数 | 整数字面量 |
| `u64` | 64 位无符号整数 | `u:N` |
| `f64` | 64 位浮点 | 浮点字面量、`!@f64` |
| `bool` | 布尔 | 比较结果、`read@bool` |
| `char` | 单字节字符 | `read@char`、`mread@char` |
| `mem` | 内存块句柄 | `alloc` / `halloc` |
| `memptr` | 指向 mem 的指针 | 字符串字面量、`&mem变量` |
| `ptr` | 变量指针 | `&变量` |
| `label` | 标签值 | `&标签名` |
| `func` | 函数值 | `&函数名` |

设计上预留了更窄的整数宽度和 `f32`，将在后续类型系统扩展中加入；
当前版本不做"假装 i64 万能"的静默降级。

### 3.2 值的表示

每个栈上的值是二元组 `(type, idx)`：类型标签 + 该类型专属运行池中的下标。
栈本身不依赖单一机器字宽。`mem/memptr/ptr` 等引用类型的 `idx` 是运行池句柄，
不是机器地址。

### 3.3 真值

需要条件的场合（`jz`/`jnz`/`assert`）接受 `bool`、`i64`、`u64`：
非零即为真。其他类型的值作为条件会直接报错。

### 3.4 数值运算的类型规则

MAD **不做隐式数值转换**：

- 算术（`+ - * / %`）：任一操作数为 `f64` 时要求**两者都是** `f64`；
  否则要求两者都是 `i64`。`u64` 参与算术需先 `!@i64`。
- 比较（`== != < > <= >=`）：要求两操作数**类型相同**且为 `i64` 或 `f64`，结果为 `bool`。
- 函数传参、`=` 赋值、`write@T` 写内存同样要求类型精确匹配。

---

## 4. 变量与作用域

### 4.1 声明即消费

对一个**未声明的合法变量名**求值时，MAD 从栈顶弹出一个值作为初值并完成声明：

```text
3 x@i64        // 声明局部/全局变量 x，初值 3
5 y            // 不写类型：y 的类型取初值的类型
```

类型注解与初值类型不一致会报错（没有隐式转换）。

对一个**已存在的变量名**求值就是加载它的值。

这条规则意味着：**同一个名字第二次出现在声明位置会报"already exists"**。
循环体内不要声明变量，先在外面声明、循环里赋值（见 15 节示例）。

### 4.2 局部与全局

- 顶层声明的变量是**全局变量**；
- 函数体（含参数）里声明的是**局部变量**，随帧销毁；
- 局部变量不得与任何全局变量同名（即使该全局未被捕获）；
- 参数不得重名、不得与全局同名。

### 4.3 全局可见性

函数只能访问其 `:{...}` 列表中声明的全局变量：

```text
:{limit}check [x@i64]
    (x limit >) too_big jnz
    ...
too_big:
    ret
;
```

顶层代码可以看到全部全局变量。

---

## 5. 内建字参考

MAD 没有"关键字"——下面这些都是解释器实现的底层字。
**用户定义的函数/标签与内建字同名时会遮蔽内建**（解析时先查用户符号表）。

### 5.1 栈操作

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `dup` | `( a -- a a )` | 复制栈顶 |
| `drop` | `( a -- )` | 丢弃栈顶 |
| `swap` | `( a b -- b a )` | 交换栈顶两项 |

### 5.2 算术

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `+` | `( a b -- a+b )` | |
| `-` | `( a b -- a-b )` | |
| `*` | `( a b -- a*b )` | |
| `/` | `( a b -- a/b )` | 除零报错 |
| `%` | `( a b -- a%b )` | 仅整数；除零报错 |

规则见 3.4。任一操作数为 `f64` 时按浮点计算（`%` 不支持 `f64`）。

### 5.3 比较

| 字 | 栈效应 |
|----|--------|
| `==` `!=` `<` `>` `<=` `>=` | `( a b -- flag )` |

结果为 `bool`。要求类型相同的 `i64` 对或 `f64` 对。

### 5.4 赋值

```text
=              ( ptr value -- )
```

先弹**指针**、再弹**值**，通过指针写入目标变量。典型用法：

```text
3 x@i64        // 声明：直接消费栈顶
5 &x =         // 赋值：x 已存在时，x = 5
v &best =      // best = v
```

### 5.5 内存

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `alloc` | `( bytes -- mem )` | 帧生命周期内存，函数返回时自动释放 |
| `halloc` | `( bytes -- mem )` | 堆生命周期，直到 `free` 或程序结束 |
| `free` | `( mem/memptr -- )` | 释放；重复释放或使用已释放内存报错 |
| `mread@T` | `( mem off -- v )` | 读标量，先弹偏移再弹内存块 |
| `write@T` | `( val mem off -- )` | 写标量，弹出顺序：偏移→内存→值 |

`T` ∈ {`i64`,`u64`,`f64`,`bool`,`char`}；宽度：i64/u64/f64 为 8 字节，bool/char 为 1 字节。
偏移+宽度越界、向只读内存写入、写入值类型不符均报错。

数组惯例（元素 `a[i]` 位于偏移 `8*i`）：

```text
(a (i 8 *) mread@i64)        // 读 a[i]
(v a (i 8 *)) write@i64      // 写 a[i] = v
```

详见第 9、15 节。

### 5.6 输入输出

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `print` / `printn` | `( v -- )` | 打印值（弹栈），无换行；`printn` 是兼容别名 |
| `println` | `( -- )` | **只打印换行，不弹栈** |
| `printstr` | `( mem/memptr -- )` | 打印 NUL 结尾的字节串 |
| `read@T` | `( -- v )` | 从标准输入读一个 T 类型的值并压栈 |

`print` 各类型的格式：i64/u64 十进制；f64 按 `%g`；bool 打印 `true`/`false`；
char 输出字节本身；mem/memptr/ptr/label/func 打印 `<type:id>` 形式的句柄。

`read@T` 支持 `i64` `u64` `f64` `char` `bool`；读取失败报错；
`bool` 接受 `true`/`false`/`1`/`0`；`char` 跳过前导空白后读一个字符。

常见坑：想"打印并换行"要写 `x print println`——`println` 自己不取值。

### 5.7 控制流与其他

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `jz` | `( flag -- )` | 为假（零）则跳转 |
| `jnz` | `( flag -- )` | 为真（非零）则跳转 |
| `jmp` / `jump` | `( -- )` | 无条件跳转（`jump` 是别名） |
| `call` | `( func -- )` | 间接调用 func 值 |
| `ret` | `( -- )` | 从当前函数返回 |
| `halt` | `( -- )` | 终止整个程序 |
| `assert` | `( flag -- )` | 为假时报 "assertion failed" |

条件跳转遵循汇编惯例：`jz` = jump if zero，`jnz` = jump if not zero。
跳转目标的写法见第 6 节。

---

## 6. 控制流与标签

### 6.1 定义与跳转

`name:` 定义标签（纯位置标记，无运行期效果）；跳转字的左侧给出目标：

```text
loop:
    ...
    (i 0 >) loop jnz     // i > 0 时回到 loop
```

条件跳转先压条件、再给目标；无条件 `jmp` 只有目标。
跳转范围限于当前函数（含顶层），不能跨函数跳。

### 6.2 标签是一等值

`&label` 产生 `label` 值，可以存进变量、经变量跳转（动态跳转）：

```text
&again hook@label            // 把标签存进变量 hook
...
(重试次数未到) hook jnz      // 经变量里的标签往回跳
...
again:
```

动态形式的目标来自栈：无条件先弹目标；条件跳转**先弹目标、再弹条件**
（所以书写顺序是"条件在前、目标在后"）。
注意：裸标签名紧挨着跳转字会被编译器**融合**成静态分支，
因此"条件 + 存储标签"的动态跳转不要再在中间写裸标签名。

### 6.3 终止

- 函数体末尾隐式 `ret`；显式 `ret` 可提前返回；
- `halt` 立即终止整个程序；
- 顶层 token 流耗尽、顶层遇到 `;`、或执行了顶层 `ret`，程序都正常结束。

---

## 7. 函数

### 7.1 定义

```text
:name [params] body ;
```

定义在扫描期登记，但**函数体不会在定义处执行**；只有被调用时才运行。

### 7.2 参数绑定

参数表 `[a@i64 b@i64]` 经词法反转后按序绑定。对调用者来说规则很简单：
**按书写顺序依次把实参压栈，然后调用**：

```text
:fact [n@i64]
    (n 2 <) base jnz
    (n 1 -) fact         // 递归：先压参数再调自身
    n *
    ret
base:
    1
;

5 fact print println     // 输出 120
```

参数按值传递；实参类型必须与形参注解**精确一致**。

### 7.3 全局捕获

访问全局变量的函数必须在 `:` 后直接列出它们（2.6）。
未列出的全局在函数体内不可见——引用它会得到"声明过的全局不存在"或
"与新局部冲突"之类的错误。

### 7.4 函数作为值

`&foo` 产生 `func` 值，可保存、传递，随后用 `call` 间接调用：

```text
:shout 65 print println ;
&shout f@func            // f = &shout，f 的类型是 func
f call                   // 间接调用，输出 65 并换行
```

`call` 要求栈顶是 `func`，实参照常先压栈。

---

## 8. 指针

`&变量` 产生指向该变量的 `ptr`（若变量持有 `mem`，则产生指向该内存块的 `memptr`，
这是 MVP 的特例）。注意 `&x` 只引用、不声明——指针要存进变量得自己声明。
`=` 通过指针写入；`*p` 形式解引用**指针变量**并加载其值：

```text
3 x@i64
&x p@ptr                 // 声明指针变量 p，初值为 &x
5 &x =                   // x = 5
*p print println         // 5：经 p 解引用
```

指针是**运行期引用**而非机器地址：它记录目标位于哪个帧（或全局表）及槽位。
解引用时校验目标帧仍然存活，因此**悬垂指针会被拒绝**而不是读到垃圾。

---

## 9. 内存模型

### 9.1 mem 对象

`mem` 是一段字节缓冲：`(data, len, heap?, readonly?)`。
`alloc`/`halloc` 弹出字节数（`i64`，可为 0，不可为负），返回 `mem` 句柄。
所有访问都做边界检查，不存在未定义读写。

### 9.2 生命周期

| 创建 | 生命周期 | 释放时机 |
|------|----------|----------|
| `alloc` | 当前函数帧 | 函数 `ret` 时自动释放（无需手动 free） |
| `halloc` | 堆 | `free` 或程序结束时统一回收 |
| 字符串字面量 | 程序全程 | 自动（只读、共享） |

在帧结束后仍持有其 `alloc` 内存的行为等同于悬垂指针，会被检测拒绝。

### 9.3 访问

标量读写用 `mread@T` / `write@T`（见 5.5）。`mread` 同时接受 `mem` 和 `memptr`。

### 9.4 字符串字面量

`"..."` 在**编译期**一次性解码为一块只读、NUL 结尾的 `mem`，运行期每次求值
得到指向它的 `memptr`——同一字面量在循环里反复出现不会反复分配。
配合 `printstr` 输出；内容不可写（write 会拒绝只读内存）。

---

## 10. 类型转换

```text
!@T            ( v -- v' )
```

弹出旧值，压入转换后的新值。允许的转换：

| 目标 \ 源 | i64 | u64 | f64 | bool | char | label/func |
|-----------|-----|-----|-----|------|------|------------|
| i64 | ✔ | ✔ | ✔ | ✔ | ✔ | |
| u64 | ✔ | ✔ | ✔ | ✔ | ✔ | ✔（取编号） |
| f64 | ✔ | ✔ | ✔ | ✔ | ✔ | |
| bool | ✔ | ✔（非零） | | | | |
| char | ✔（截断） | ✔（截断） | | | | |
| 其他 | ✘ | ✘ | ✘ | ✘ | ✘ | ✘ |

同类型转换是恒等。整数→label 被刻意禁止（防止伪造跳转目标）。

---

## 11. 输入输出细节

标准输入按 `read@T` 的类型解析：

| 类型 | 规则 |
|------|------|
| `i64` | 十进制整数（`scanf` 语义） |
| `u64` | 十进制无符号整数 |
| `f64` | 浮点数 |
| `char` | 跳过空白后读一个字节 |
| `bool` | 一个词，`true`/`false`/`1`/`0` |

读取失败（EOF 或格式不符）立即报错终止。

标准输出是无缓冲刷新依赖的逐字符 printf；`print` 系列都不追加空格或换行，
分隔符需要自己输出（如 `" " printstr`）。

---

## 12. 错误处理

所有运行期/编译期错误（类型不符、栈下溢、越界、除零、悬垂指针、断言失败……）
都走同一条路径：**打印 `文件:行号: 错误信息` 后立即退出**，非零退出码。
没有异常、没有恢复；这是原型语言的刻意取舍。

---

## 13. 执行模型与实现原理

### 13.1 两遍处理，无 AST

解释器从不构建语法树。启动后：

1. **扫描期**（symtab）：线性扫一遍 token 流，登记每个函数的切片
   （名字、参数表、`:{}` 捕获、函数体的 token 区间）以及每个函数体内的标签位置；
2. **执行期**：把剩余顶层 token 流当作匿名 `<top>` "函数"编译并执行。

### 13.2 Threaded Code

每个函数体在**首次被调用时**惰性编译成一个指令数组（`Op`），并缓存复用。
每条指令的核心字段是 `code`——一个 GCC labels-as-values 的 `&&label` 地址，
派发就是一条 computed goto：

```c
#define NEXT() goto *(++ip)->code
```

相比早期版本"逐 token 走一遍、每个字做一长串 strcmp"的方式，
字面量、名字、调用目标、跳转目标都在编译期一次解析完毕，热路径只剩
取指—派发—执行。基准（fib(27) 递归 + 200 万次迭代循环）约提速 5 倍。

编译期完成的还有：字符串字面量一次性物化为共享只读 mem；
`label jz/jnz/jmp` 相邻序列**融合**为一条直接分支指令
（不再运行期压/弹 label 值）；无法静态确定目标的动态跳转使用独立的
`*_DYN` 指令变体，运行期从栈上取目标。

### 13.3 名字解析优先级

编译一个字时按以下顺序判定（这与旧版逐 token 解释的运行期顺序一致）：

```text
字面量 → 标签定义(name:) → &name / *name / !@T → 标签名 → 函数名
→ 跳转融合检查 → ret/halt/dup/drop/swap → 算术 → 比较 → '='
→ call / jz / jnz / jmp / jump → mread@ / write@ / read@
→ alloc / halloc / free → print / printn / println / printstr / assert
→ 兜底：变量加载或隐式声明
```

注意**函数名先于内建字查询**，所以用户定义会遮蔽同名内建。

### 13.4 变量的存在性是运行期属性

编译器不要求变量预先声明：裸变量名编译成一条"加载或声明"指令，
运行时先查作用域，查不到就把栈顶弹出来当初值。
因此"某个变量是否存在"可以取决于执行路径（例如只在某个分支里声明过）。
这是刻意保留的动态语义，也是本语言名字里 "Ain't Disciplined" 的一部分。

### 13.5 数据结构总览

| 结构 | 作用 |
|------|------|
| token 流 | 词法产物，唯一中间表示 |
| FuncSym | 函数：参数、捕获列表、标签表、编译缓存 |
| Value / 类型池 | `(type, idx)` 二元组 + 按类型分池存储 |
| Frame | 调用帧：局部变量表 + 本帧 alloc 内存清单 |
| MemObj | 内存块：data/len/heap/readonly |
| 数据栈 | 所有操作数的中转站 |

---

## 14. 代码风格指南

参考范本：`examples/nqueens.mad`、`examples/p1038.mad`。

1. **一行一个完整操作**。操作不多时，每行做一个相对完整的动作，
   不要把整个程序挤成一坨平铺 token。
2. **最外层不加括号，内层要加括号**。一行里最外层的那个操作不加包裹括号
   （保持清爽，避免 Lisp 式括号地狱）；它操作数的子表达式用 `( )` 圈起来：

   ```text
   (board (r 8 *) mread@i64) &cell =    // cell = board[r]，最外层是 =
   (row 1 +) solve                      // solve(row + 1)，最外层是调用
   ```

3. **特别长的操作用跨行括号形式**，缩进表达嵌套层次：

   ```text
   (
       data/op data/op (
           data/op data/op ...
       )
       ...
   )
   ```

   例如：

   ```text
   (
       (xs (i 8 *) mread@i64)
       (ys (j 8 *) mread@i64)
       *
   ) s + &s =                        // s += xs[i] * ys[j]
   ```

4. 控制流的惯用形状（汇编风）：

   ```text
   cond_label:
       (条件) done jnz          // if (!cond) goto done
       ...
       cond_label jmp           // 回到条件
   done:
   ```

5. 声明集中在作用域开头；**循环里只赋值，不声明**（见 4.1）。
6. `//` 注释解释"这一行在做什么高层的事"，尤其是跳转条件。

---

## 15. 完整示例

读入 `n` 和 `n` 个整数，输出最大值及其下标（0 起）。

```text
// max.mad
read@i64 n@i64
(n 8 *) alloc a             // a[i] 在偏移 8*i
0 i@i64
0 v@i64
0 best@i64
0 bi@i64

scan:
    (i n >=) report jnz     // 循环条件
    read@i64 &v =
    (v best >) take jnz
scan_next:
    (i 1 +) &i =
    scan jmp

take:
    v &best =
    i &bi =
    scan_next jmp

report:
    best print
    " " printstr
    bi print
    println
halt
```

输入 `5` 与 `3 9 1 7 2` 时输出 `9 1`。

演示了：typed read、alloc 数组、集中声明 + 循环赋值、标签循环、
比较跳转、字符串分隔符。更多范例（递归、二维矩阵、图遍历、间接调用）见
`examples/nqueens.mad` 与 `examples/p1038.mad`。
