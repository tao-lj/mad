# MAD 语言手册

MAD（**MAD Ain't Disciplined**）以后缀记法 + 数据栈为核心，无语句/表达式之分：
字面量压栈，字（word）弹栈操作后压回结果。书写顺序即执行顺序。

栈效应记号：`( 弹出顺序 -- 压入顺序 )`，如 `+` 是 `( a b -- sum )`（b 先弹）。

完整类型/字面量/运算符表：[`REFERENCE.md`](REFERENCE.md)

## 1. 词法结构

- 注释：`//` 到行尾；空白仅分隔 token。
- `( )` 被词法器丢弃，纯视觉分组：`(a b +) c *` ≡ `a b + c *`。
- `[ ]` 收集其中 token **整体反转**放回流（不可嵌套），用于参数表和批量声明。
- 变量名以字母或 `_` 开头，后续可为字母、数字、`_`、`@`。前缀记法作为独立 token：

  | 形式 | 含义 |
  |------|------|
  | `&name` | 引用：变量→ptr，标签→label，函数→func |
  | `*name` | 解引用指针变量 |
  | `!@T` | 类型转换 |
  | `name:` | 标签定义 |
  | `u:N` | 无符号字面量 |
  | `T:N` | 定宽字面量（如 `i8:10`、`f32:3.14`、`char:65`） |

- 字面量：`123` / `-3` → i64；`3.14` → f64；`u:42` → u64；`"text"` → memptr
  （转义 `\n \r \t \0 \\ \"`）。`'c'` → 字符字面量（转义 `\n \0 \\ \'`），
  存储为 i8。无布尔字面量。最长 255 字节。
  注意：`-3` 是一个整体 token，取负请写 `(0 x -)`。
- 定义：`:name ... ;` 函数（定义时注册、调用时执行）；`:{globals}name ... ;` 捕获全局；
  顶层 `;` 停止执行。
- 类型注解：任何变量名可带 `@type` 后缀（如 `x@i64`）。

## 2. 类型

常用标量类型（完整列表见 [`REFERENCE.md`](REFERENCE.md)）：

| 类型 | 宽度 | 示例 |
|------|------|------|
| `i64` | 8 | `123`（整数默认） |
| `u64` | 8 | `u:42` |
| `f64` | 8 | `3.14`（浮点默认） |
| `bool` | 1 | 比较结果、`!`、`&&`、`\|\|` |
| `char` | 1 | `'c'` |

句柄类型：`mem`、`memptr`、`ptr`、`label`、`func`、`file`。

所有标量值内联存储于 `Value` 联合体——无池间接引用。句柄存储 uint64 id。

### 算术与比较规则

- 同类型：`i64 + i64 → i64`，`f64 * f64 → f64`。
- 混合数值：较小整数提升为 i64；任一 f64 则双方转 f64。
- `%` 仅支持整数（不支持 f32/f64）。
- 位运算（`~ << >> & | ^`）仅接受整数类型（i8..u64、bool、char），结果 i64。
- 逻辑运算（`! && ||`）接受任意标量，结果 bool。
- 比较要求数值类型，结果 bool。
- **无隐式赋值转换**——要求精确类型匹配。

## 3. 变量与作用域

**声明即消费：**

```mad
3 x@i64          // 声明 x 为 i64，值为 3
x                // 加载 x
3 &x =           // 赋值 x = 3
```

未声明的合法变量名弹出栈顶作为初值并声明。已存在的名字求值即加载。禁止二次声明。

- 顶层声明为全局变量；函数体内为局部变量（帧生命周期）。
- 局部变量和参数不得与全局同名；参数之间不得重名。
- 函数只能访问 `:{...}` 列表中列出的全局变量。

```mad
1 2 3 [a b c]    // 批量声明：a=1 b=2 c=3
```

## 4. 内建字

用户定义的函数/标签**遮蔽同名内建**。

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `dup` / `drop` / `swap` | 栈操作 | 复制 / 丢弃 / 交换栈顶 |
| `+ - * / %` | `( a b -- r )` | 算术；除零报错 |
| `== != < > <= >=` | `( a b -- flag )` | 比较，结果 bool |
| `~` | `( a -- r )` | 按位取反（整数，→ i64） |
| `!` | `( a -- flag )` | 逻辑取反（任意标量，→ bool） |
| `<<` / `>>` | `( a b -- r )` | 移位（整数，→ i64） |
| `&` `\|` `^` | `( a b -- r )` | 按位与/或/异或（整数，→ i64） |
| `&&` `\|\|` | `( a b -- flag )` | 逻辑与/或（任意标量，→ bool） |
| `=` | `( ptr value -- )` | 经指针赋值 |
| `alloc` / `halloc` | `( bytes -- mem )` | 帧 / 堆生命周期内存 |
| `free` | `( mem/memptr -- )` | 释放内存 |
| `sizeof` | `( v -- i64 )` | 标量返回类型字节数；mem/memptr 返回 mem 长度 |
| `mread@T` | `( mem off -- v )` | 读标量 |
| `write@T` | `( val mem off -- )` | 写标量 |
| `print` / `printn` | `( v -- )` | 打印值不换行 |
| `println` | `( -- )` | 打印换行 |
| `printstr` | `( mem/memptr -- )` | 打印 NUL 结尾字符串 |
| `read@T` | `( -- v )` | 读类型化 stdin 值 |
| `call` | `( func -- )` | 间接调用 |
| `ret` / `halt` | `( -- )` | 返回 / 终止 |
| `assert` | `( flag -- )` | 为假则报错 |
| `import` | `( path -- )` | 导入模块 |
| `fopen` | `( path mode -- file )` | 打开文件；失败则报错 |
| `fclose` | `( file -- )` | 关闭文件句柄 |
| `fsize` | `( file -- i64 )` | 查询文件大小（不移动位置） |
| `fread` | `( file n -- count mem )` | 读取最多 n 字节；count≤n |
| `fwrite` | `( buf file -- written )` | 写入 buf 全部内容；返回写入字节数 |
| `system` | `( cmd -- rc )` | 执行 shell 命令；rc 为原始 `waitpid` 状态值 |

`T` ∈ {i8, u8, i16, u16, i32, u32, i64, u64, f32, f64, bool, char}。

## 5. 控制流与函数

**标签与跳转：**

```mad
loop:
    ...
    (i 0 >) loop jnz    // 条件在前、目标在后；jz=零跳，jnz=非零跳
```

`jmp`/`jump` 无条件跳转。`&label` 产生一等 label 值。裸标签名与跳转字相邻时
被编译期**融合**为直接分支。

**函数：**

```mad
:fact [n@i64]
    (n 2 <) base jnz
    (n 1 -) fact
    n *
    ret
base:
    1
;
```

参数经 `[]` 反转按书写顺序绑定，按值传递且精确类型匹配。`;` 隐式 `ret`。
`&foo` 产生 `func` 值，用 `call` 间接调用。

## 6. 指针与内存

`&变量` 产生 `ptr`（变量持 mem 时产生 `memptr`）。`=` 经指针写入。`*p` 解引用。

```mad
3 x@i64   &x p@ptr   5 &x =   *p print println    // 输出 5
```

指针记录目标帧，解引用校验帧存活，悬垂指针被拒绝。

**内存：**

| 创建 | 生命周期 |
|------|----------|
| `alloc` | 帧生命周期；函数 ret 时自动释放 |
| `halloc` | 堆生命周期；`free` 或程序结束释放 |
| 字符串字面量 | 只读，程序生命周期 |

标量访问：`m offset mread@T` / `value m offset write@T`。
数组约定：按元素大小偏移——`(a (i ELEM_SIZE *) mread@T)` 读 `a[i]`。

## 7. 类型转换

`!@T` 弹出旧值压入新值。允许的转换：

| 源 → 目标 | 说明 |
|-----------|------|
| 整数 ↔ 整数 | 截断到目标宽度 |
| 整数 ↔ f32/f64 | 截断或转换 |
| bool → 整数/f32/f64 | false→0, true→1 |
| 整数/char/float → bool | 非零为 true |
| 整数 ↔ char | 截断为单字节 |
| label/func → u64 | 取内部编号 |
| 同类型 | 恒等 |

整数 → label 刻意禁止。

## 8. 模块导入

```mad
"lib.mad" import
```

弹出 memptr 路径，读取并执行另一个 MAD 文件：函数定义被注册，顶层运行后
其全局和函数对导入方可见。

- 相对路径基于**导入方文件目录**解析。
- 只允许顶层 `import`。
- 每个文件至多导入一次（`#pragma once` 语义，默认开启）。
- 嵌套深度上限 64。

## 9. 输入输出与错误

stdin 按 `read@T` 解析：整数十进制、浮点按格式、char 跳空白读一字节、
bool 接受 `true/false/1/0`。失败报错。stdout 不自动加分隔符。
`print` 对句柄输出 `<type:id>` 形式。

**文件 I/O：**

`fopen` 接受两个 mem 字符串（路径和模式，如 `"r"`、`"w"`），返回 `file`
句柄。所有文件操作失败时直接报错（打开失败、句柄已关闭、无效 id）。
`fread` 分配 frame-lifetime 缓冲区读取最多 `n` 字节；`fwrite` 写入整个
mem/memptr 缓冲区。

`fsize` 通过 `fseek`/`ftell` 查询文件大小，不改变当前位置。文件句柄在程序
退出时自动关闭。

```mad
"/tmp/out.txt" "w" fopen f@file
"hello" f fwrite drop
f fclose

"/tmp/out.txt" "r" fopen g@file
g fsize n@i64
g n fread data@mem drop
data printstr println
g fclose
```

所有错误打印 `行号: 信息` 后立即退出，无异常无恢复。

## 10. 实现原理

**两遍处理，无 AST：** 扫描 token 流登记函数切片和标签位置，编译剩余顶层
为匿名 `<top>` 函数。`import` 在运行时追加 token 并重扫描。

**线程码：** 函数体首次执行时惰性编译为 `Op` 数组。使用 GCC labels-as-values
（`&&label`），派发为一条 computed goto。字面量、名字、调用目标、跳转目标
编译期解析；`label jz/jnz/jmp` 融合为直接分支。

**编译期类型栈：** builder 在构建 IR 时追踪操作数类型。当两个操作数类型已知
且兼容时，发射**类型化 IR**（如 `IR_ADD_I64`、`IR_EQ_F64`），降级为
**类型化 opcode**（`OP_ADD_I64`、`OP_EQ_F64`）——零开销快速路径。
类型未知时走**泛型 opcode**（`OP_ADD`、`OP_EQ`），运行时分派。

**IR 流水线：** `ir_build` → `ir_optimize`（常量折叠）→ `ir_check`
（工作流类型检查，通过 `ir_apply`）→ `ir_lower`（IR → 线程码 Op 数组）。
