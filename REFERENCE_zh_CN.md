# MAD 参考手册

类型、字面量和运算符的完整表格。用法详见 [`MANUAL_zh_CN.md`](MANUAL_zh_CN.md)。

## 标量类型

| 类型 | 宽度（字节） | 有符号 | 范围 / 说明 |
|------|-------------|--------|-------------|
| `i8` | 1 | 是 | -128..127 |
| `u8` | 1 | 否 | 0..255 |
| `i16` | 2 | 是 | -32768..32767 |
| `u16` | 2 | 否 | 0..65535 |
| `i32` | 4 | 是 | -2^31..2^31-1 |
| `u32` | 4 | 否 | 0..2^32-1 |
| `i64` | 8 | 是 | -2^63..2^63-1（默认整数） |
| `u64` | 8 | 否 | 0..2^64-1 |
| `f32` | 4 | — | IEEE 754 单精度 |
| `f64` | 8 | — | IEEE 754 双精度（默认浮点） |
| `bool` | 1 | — | false / true |
| `char` | 1 | — | 单字节字符（I/O 中等价于 i8） |

## 句柄类型

| 类型 | 含义 | 创建方式 |
|------|------|----------|
| `mem` | 字节缓冲句柄 | `alloc` / `halloc` |
| `memptr` | 指向 mem 的指针 | 字符串字面量、`&mem变量` |
| `ptr` | 变量指针 | `&变量` |
| `label` | 一等标签值 | `&label` |
| `func` | 一等函数值 | `&函数名` |
| `file` | 打开的文件句柄 | `fopen` |

## 字面量

| 语法 | 类型 | 示例 |
|------|------|------|
| `123` / `-3` | i64 | `123`、`-7` |
| `u:42` | u64 | `u:0xFF` |
| `3.14` | f64 | `-1.0`、`2e10` |
| `"text"` | memptr | `"hello\n"` |
| `'c'` | char（i8） | `'A'`、`'\n'` |
| `i8:N` | i8 | `i8:10` |
| `u8:N` | u8 | `u8:255` |
| `i16:N` | i16 | `i16:-1000` |
| `u16:N` | u16 | `u16:65535` |
| `i32:N` | i32 | `i32:100000` |
| `u32:N` | u32 | `u32:4000000000` |
| `u64:N` | u64 | `u64:100` |
| `f32:N` | f32 | `f32:3.14` |
| `f64:N` | f64 | `f64:3.14` |
| `char:N` | char（i8） | `char:65` |

### 转义序列（字符串和字符字面量）

| 序列 | 含义 |
|------|------|
| `\n` | 换行 |
| `\r` | 回车 |
| `\t` | 制表符 |
| `\0` | 空字节 |
| `\\` | 反斜杠 |
| `\"` | 双引号 |
| `\'` | 单引号 |

## 运算符

### 算术

| 运算符 | 栈效应 | 类型 | 结果 |
|--------|--------|------|------|
| `+` | `( a b -- r )` | 数值 | 同类型 |
| `-` | `( a b -- r )` | 数值 | 同类型 |
| `*` | `( a b -- r )` | 数值 | 同类型 |
| `/` | `( a b -- r )` | 数值 | 同类型 |
| `%` | `( a b -- r )` | 仅整数 | i64 |

类型化 opcode：`OP_ADD_I64`、`OP_ADD_F64`、`OP_SUB_I64`、`OP_SUB_F64`、
`OP_MUL_I64`、`OP_MUL_F64`、`OP_DIV_I64`、`OP_DIV_F64`、`OP_MOD_I64`。
泛型回退：`OP_ADD`、`OP_SUB`、`OP_MUL`、`OP_DIV`、`OP_MOD`。

### 比较

| 运算符 | 栈效应 | 类型 | 结果 |
|--------|--------|------|------|
| `==` | `( a b -- flag )` | 数值 | bool |
| `!=` | `( a b -- flag )` | 数值 | bool |
| `<` | `( a b -- flag )` | 数值 | bool |
| `>` | `( a b -- flag )` | 数值 | bool |
| `<=` | `( a b -- flag )` | 数值 | bool |
| `>=` | `( a b -- flag )` | 数值 | bool |

类型化 opcode：`OP_EQ_I64`、`OP_EQ_F64`、`OP_NE_I64`、`OP_NE_F64`、
`OP_LT_I64`、`OP_LT_F64`、`OP_GT_I64`、`OP_GT_F64`、
`OP_LE_I64`、`OP_LE_F64`、`OP_GE_I64`、`OP_GE_F64`。
泛型回退：`OP_EQ`、`OP_NE`、`OP_LT`、`OP_GT`、`OP_LE`、`OP_GE`。

### 位运算

| 运算符 | 栈效应 | 类型 | 结果 |
|--------|--------|------|------|
| `~` | `( a -- r )` | 整数 | i64 |
| `<<` | `( a b -- r )` | 整数 | i64 |
| `>>` | `( a b -- r )` | 整数 | i64 |
| `&` | `( a b -- r )` | 整数 | i64 |
| `\|` | `( a b -- r )` | 整数 | i64 |
| `^` | `( a b -- r )` | 整数 | i64 |

整数类型：i8、u8、i16、u16、i32、u32、i64、u64、bool、char。
类型化 opcode：`OP_SHL_I64`、`OP_SHR_I64`、`OP_AND_I64`、`OP_OR_I64`、`OP_XOR_I64`。
泛型回退：`OP_SHL`、`OP_SHR`、`OP_AND`、`OP_OR`、`OP_XOR`。

### 逻辑

| 运算符 | 栈效应 | 类型 | 结果 |
|--------|--------|------|------|
| `!` | `( a -- flag )` | 任意标量 | bool |
| `&&` | `( a b -- flag )` | 任意标量 | bool |
| `\|\|` | `( a b -- flag )` | 任意标量 | bool |

### 赋值和内存

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `=` | `( ptr value -- )` | 经指针赋值 |
| `alloc` | `( bytes -- mem )` | 帧生命周期分配 |
| `halloc` | `( bytes -- mem )` | 堆生命周期分配 |
| `free` | `( mem/memptr -- )` | 释放内存 |
| `sizeof` | `( v -- i64 )` | 值的字节数 |
| `mread@T` | `( mem off -- v )` | 从 mem 读标量 |
| `write@T` | `( val mem off -- )` | 向 mem 写标量 |

### 类型转换

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `!@T` | `( old -- new )` | 转换为类型 T |

允许的转换：整数↔整数（截断）、整数↔浮点、bool→整数、
整数/char/浮点→bool、整数↔char、label/func→u64、同类型→同类型（无操作）。
整数→label 被禁止。

### 输入输出

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `print` / `printn` | `( v -- )` | 打印值，不换行 |
| `println` | `( -- )` | 仅打印换行 |
| `printstr` | `( mem/memptr -- )` | 打印 NUL 结尾字符串 |
| `read@T` | `( -- v )` | 从 stdin 读类型化值 |

### 文件 I/O

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `fopen` | `( path mode -- file )` | 打开文件；失败则报错 |
| `fclose` | `( file -- )` | 关闭文件句柄 |
| `fsize` | `( file -- i64 )` | 查询文件大小（不移动位置） |
| `fread` | `( file n -- count mem )` | 读取最多 n 字节；count≤n |
| `fwrite` | `( buf file -- written )` | 写入 buf 全部内容；返回写入字节数 |

### 外部交互

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `system` | `( cmd -- rc )` | 执行 shell 命令；rc 为原始 `waitpid` 状态值 |

### 控制流

| 字 | 栈效应 | 说明 |
|----|--------|------|
| `ret` | `( -- )` | 从函数返回 |
| `halt` | `( -- )` | 终止程序 |
| `assert` | `( flag -- )` | 为假则中止 |
| `call` | `( func -- )` | 间接函数调用 |
| `import` | `( path -- )` | 导入模块 |
| `jmp` / `jump` | `( -- )` | 无条件跳转 |
| `jz` | `( condition -- )` | 为零/false 时跳转 |
| `jnz` | `( condition -- )` | 非零/true 时跳转 |

条件接受 bool 和所有整数类型（i8..u64），不接受浮点和句柄类型。

### 栈操作

| 字 | 说明 |
|----|------|
| `dup` | `( a -- a a )` 复制栈顶 |
| `drop` | `( a -- )` 丢弃栈顶 |
| `swap` | `( a b -- b a )` 交换栈顶两项 |
